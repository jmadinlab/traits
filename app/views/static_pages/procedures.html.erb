<% provide(:title, 'Procedures') %>

<div class="row">
  <div class="col-md-8 col-md-offset-2">

    <% if signed_in? && current_user.editor? %>
      <%= markdown File.open(Rails.root + "README.md", 'r').read %>
    <% else %>
      <p>Under review</p>
    <% end %>

  </div>
</div>