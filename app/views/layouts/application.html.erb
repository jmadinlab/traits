<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>  
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= analytics_init :local => Rails.env.development? %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    

    <div class="container">
          <%= render 'layouts/header' %>

          <!-- <div class="alert alert-danger" role="alert">The Coral Traits Database is under review for release in approximately November 2014. Please feel free to use the database, but be aware that changes might occur.</div> -->

          <% flash.each do |key, value| %>
            <div class="alert alert-<%= key %>"><%= value %></div>
          <% end %>

          <%= yield %>
        	
          <%# render partial: "layouts/google_analytics" %>

          <%#= render 'layouts/footer' %>
          <%#= debug(params) if Rails.env.development? %>
        </div>

  </body>
</html>
