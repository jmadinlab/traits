<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>New Release</h1>

    <p>Previous release: <span class="label label-primary"><%= Release.all.map(&:release_code).max %></span></p>

    <%= render 'form' %>

    <%= link_to 'Back', releases_path %>
  </div>
</div>
